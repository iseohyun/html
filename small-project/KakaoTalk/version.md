# Version History (version.md)

## [v1.1.0] - 진행 중 (Next Release)
### Added (새 기능)
- **그림 다운로드 기능 확장**: 다운로드(⬇) 버튼 클릭 시의 동작 고도화
  - "어떤 상태를 저장할까요?" 
  1. 지금의 정지상태
  2. 애니매이션(애니메이션의 시작과 끝을 기준)
  3. 전체 그림

---

## [v1.0.6] - SPA 환경 구동 최적화 및 레이아웃 리사이징 디버그 패치
### Fixed (버그 수정)
- **SPA 환경 화면 레이아웃 및 2단 리사이징 영역 오류 근본 개선**:
  - 포털 라우터의 innerHTML 주입 시 id 유실 문제를 타개하기 위해 `<article>` 내부에 전용 `#kakaotalk-article` div 컨테이너를 새로 씌워 캡슐화 완료. 이에 따라 데스크톱 800px 이상 공간에서 2단 분할 레이아웃이 유실 없이 정상 가동되도록 복원.
  - `#svg-box` 에 걸려있던 `max-width: 500px` 한계를 해제하여 사이드바 열림/닫힘 시 가용폭이 100% 동적 추적되도록 반응성 보장.
  - `ResizeObserver` 및 `window.KakaoTalkMain.wakeRenderer()` API를 연동하여, 창 크기나 메뉴바 개폐 시 캔버스 물리 해상도와 CSS 뷰포트 크기가 비율(Aspect Ratio)을 깨지 않고 정비율로 실시간 연동 리렌더링되도록 보정 완료.
  - 카카오톡 페이지 이탈(타 페이지 이동) 시 `ResizeObserver`와 `requestAnimationFrame` 루프가 스스로 `disconnect()` 및 `cancelAnimationFrame`을 수행하여 사멸하게 만드는 **자가 치유 소멸(Self-Healing Destructors) 기술**을 적용해 다른 서브페이지들의 클릭/여백 오염 장애를 완전 해결.
  - kakaotalk-style.css 의 body 선택자 오버라이딩을 완전 폐지하여, 타 페이지 전환 시 전역 사이드바 본물 밀기 패딩 여백이 오염되는 에러 완전 차단.
  - **SPA 해시 라우팅 환경 리소스 페치 오류 수정**: `getAbsoluteUrl` 유틸리티를 도입해 help.html, default-setting.md, example1.txt 경로 절대화 완료.
  - **SPA 재진입 먹통 해결**: DOMContentLoaded 결합 코드를 `window.KakaoTalkMain.init` API로 래핑하고, index.html 하단에 Promise 순차 비동기 로더 체인을 장착하여 재진입 시에도 매번 정상 바인딩 및 기동 보장.

### Changed (변경사항)
- **도움말 지원**: help.html의 내용을 변수로 로드하여 세련된 모달 디자인 팝업으로 표출.

---

## [v1.0.4] - 재생 속도 초 단위화 및 내부 고정 헤더 스크롤 지원
### Added (새 기능)
- **고정 헤더 및 내부 본문 스크롤 적용**: 컨테이너 `#svg-box`에 세로 800px 크기 제한 및 overflow: hidden을 걸고, 캔버스 드로잉 시 Y: 240px 헤더 하단에만 본문 말풍선들이 스크롤 변위를 받아 렌더링되도록 2D 클리핑 마스크 연동 완료
- **재생 속도 범위 재정립**: 대화 속도 슬라이더 범위를 0~2초(0.1초 단위, 기본 0.5초)로 미세 조절하게 하고 우측 라벨에 `X.X초` 형태로 연동 완료

### Fixed (버그 수정)
- **화면 비율 미적용 문제 디버그**: 가로/세로 input number 아래에 실시간 픽셀 수치(px)를 출력하고 캔버스 크기를 리사이징해 리렌더링하도록 수정
- **화자 이름 중복 노출 개선**: 연속 메시지 발송 시 화자 이름을 1회만 노출하도록 pos.isContinuous 조건절 가드 추가 및 좌표 보정
- **기타 UI 편의성 패치**: help.html 도움말 세팅 방법에 "스크롤 고정" 체크박스 해제 방법 설명 추가 및 문구 통일

---

## [v1.0.3] - 모듈 삼분할 및 UI 고도화
### Added (새 기능)
- **스크립트 역할 분할 리팩토링**: 그래픽 드로잉/엔진(`engine.js`), UI 제어/바인딩(`interface.js`), 시뮬레이션 라이프사이클(`main.js`) 삼분할 완료
- **환경설정 UI 상세 제어**: 환경설정 아코디언 기본 접힘 상태(collapsed) 적용 및 글꼴 상세(크기, 굵게) 옵션 추가
- **말풍선 테마 커스텀**: 내 말풍선(#ffeb34), 상대 말풍선(#ffffff), 대화 시간(#cccccc) 개별 색상 피커 지원 및 hh:mm 꼬리 표시 연동

---

## [v1.0.2] - 대화 1step 버튼 및 글꼴 변경 지원
### Added (새 기능)
- **대화 1step 버튼 추가**: "애니메이션 설정"의 "재생" 버튼 옆에 "한 스텝(▶⎸)" 버튼 추가하여 대화를 1개씩 순차 진행하는 기능 지원
- **글꼴 변경 지원**: "환경설정"에 시스템 글꼴을 동적으로 가져올 수 있는 "글꼴" 선택 항목 추가

---

## [v1.0.1] - 간단한 UI 수정
### Changed (변경사항)
- **메뉴 명칭 통일**: "외부 요인 및 환경설정" 카테고리명을 "환경설정"으로 심플하게 변경

---

## [v1.0.0] - SVG -> Canvas 전환 및 애니메이션 엔진 고도화
### Added (새 기능)
- **HTML5 Canvas 2D 그래픽 렌더링 엔진 전면 전환**: 기존 SVG DOM 통짜 주입 방식의 깜빡임(Flickering) 문제 원천 차단
- **60fps 하드웨어 가속 렌더러 탑재**: `requestAnimationFrame` 프레임 루프를 기반으로 투명도, 블러, 3방향 슬라이드, 리얼 글자 슬라이싱 타이핑 등장 효과 지원
- **스무스 스크롤링 탑재**: 스크롤 이동 시 목표 지점을 부드럽게 추적해 이동하는 Lerp(Linear Interpolation) 지수 감속 알고리즘 적용

---

## [v0.3.1] - 프로필 이미지 입력 기능
### Added (새 기능)
- **이미지 인스턴스 고속 캐싱 모듈 구축**: 비동기로 읽어들인 Base64 프로필 이미지 데이터를 메모리에 즉시 매핑하는 구조 설계

---

## [v0.3.0] - 다운로드 방식 변경 (PNG)
### Added (새 기능)
- **무손실 고화질 PNG 파일 캡처 다운로드**: 다운로드(⬇) 클릭 시 폰트 손실이나 레이아웃 꼬임이 없는 고화질 스냅샷 출력 기능 구현

---

## [v0.2.5] - 애니메이션 효과 미적용 버그 개선
### Fixed (버그 수정)
- **말풍선 등장 효과 기능 구현 및 디버그**:
  - 투명도 (0 -> 1 페이드인)
  - 타이핑 (말풍선은 즉시 노출되나 텍스트에 타이핑 효과 부여)
  - 흐리게 (blur 필터가 점진적으로 해제되는 효과)
  - 밀어내기 (말풍선이 프로필 초상화 방향으로부터 슬라이드 인)

---

## [v0.2.4] - 스크롤 고정 미적용 버그 개선
### Fixed (버그 수정)
- **스크롤 설정 적용 디버그**: 대화 스크롤 설정 체크 시, 지정된 가상 대화 영역 내에서 대화 내용이 부드럽게 밀려 올라가도록 수정

---

## [v0.2.3] - 상대 초상화 미적용 버그 개선
### Fixed (버그 수정)
- **초상화 매핑 로직 수정**: 대화 상대 초상화 상세 설정에 입력된 메타데이터를 기반으로 캔버스 내 상대방 프로필 이미지가 동적 매핑되도록 보정

---

## [v0.2.2] - 작은 UI 수정
### Changed (변경사항)
- **재생/정지 버튼 단일화**: 시뮬레이션 플레이 버튼과 정지 버튼을 하나로 통합하여 토글식 제어로 간소화

---

## [v0.2.1] - 환경설정 기능 고도화
### Added (새 기능)
- **설정 저장 및 입출력 모듈**: 환경설정 데이터 로컬 저장 및 관리를 위한 초기화(↺), 저장(⬇), 불러오기(⬆) 기능 탑재
- **화자 선택 바인딩**: '내 이름(나)' 설정 시 대화 목록 내 등장인물 중 동적 선택(Select)할 수 있도록 연동 (없음 포함)

---

## [v0.2.0] - 대화 상대 설정 기능 추가
### Added (새 기능)
- **대화 상대 초상화 설정 지원**:
  - 위치: 내 이름 항목 하단 배치
  - '상세 설정 열기' 팝업 모달을 통해 대화 상대방의 개별 배경색, 글자, 글자색 커스터마이징 기능 추가 (기본 팔레트 8종 제공)
- **환경설정 UI 노출 순서 전면 개편**:
  1. 캡처 시간 -> 2. 대화방 이름 -> 3. 대화방 배경색 -> 4. 배터리 상태 -> 5. 와이파이 상태 -> 6. 셀 상태 -> 7. 내 이름 -> 8. 대화상대 초상화

---

## [v0.1.0] - 애니메이션 기능 추가
### Added (새 기능)
- **진행률 표기 세분화**: 대화 진행률을 백분율(%) 대신 '현재 인덱스 / 총 대화 수' 포맷으로 직관적으로 변경
- **스크롤 강제 고정 옵션**: 스크롤 유지 설정 체크 시 기본 UI 영역은 고정한 채 마지막 대화 기준 하단 정렬 유지
- **TTS(읽어주기) 옵션**: 애니메이션 흐름에 맞추어 실시간으로 텍스트를 음성 변환(TTS)하여 읽어주는 기능 추가
- **말풍선 등장 속도 세분화**: 등장 애니메이션 속도 옵션 제공 (없음 / 천천히 0.5초 / 빠르게 0.2초)

---

## [v0.0.9] - 배경색상 적용 버그 수정 및 컨트롤 박스 전개
### Fixed (버그 수정)
- **설정값 로드 오류 해결**: default-setting.md 파일 내 `setting-bgcolor: #bacee0` 키값을 정상적으로 파싱하지 못하던 버그 패치

---

## [v0.0.8] - 컨트롤 박스 디자인 변경 및 파일 업로더 구현
### Changed (변경사항)
- **레이아웃 2분할 분리**: 화면 좌측은 드로잉 영역, 우측은 컨트롤 패널로 역할군 분리
- **입력 컴포넌트 편의성 개선**:
  - 와이파이, 셀, 배터리 상태 등의 입력을 range(슬라이더) 컨트롤로 변경하고 정밀도 세분화 (0%, 25%, 50%, 75%, 100%) 및 수치 표시
  - 배경색 선택에 전용 컬러 피커 적용 및 하단 고정 배치
  - 캡처 시간 설정을 대화 로그의 "저장한 날짜" 기반 데이터와 연동하고 Day-Time Picker로 제어 유도
  - 각 환경설정 카테고리를 아코디언 타이틀 클릭 형태로 접고 펼칠 수 있게 수정
- **파일 직접 로드 모듈**: 대화내용 불러오기 클릭 시 OS 파일 선택창을 띄워 로컬 대화 내용을 즉시 텍스트 영역에 출력하도록 처리

---

## [v0.0.7] - UI 배치 변경 및 CSS 구조화
### Changed (변경사항)
- **실시간 예제 노출**: 텍스트 입력창(`chat-input`)에 기본 탑재된 `example1.txt` 데이터를 실시간으로 출력하도록 기능 변경
- **CSS Grid 레이아웃 적용**: index.html 내부의 인라인 스타일(`float`, `width`)을 모두 걷어내고, CSS Grid 속성을 활용하여 화면을 좌/우 2열로 완전 대칭 구조화

---

## [v0.0.6] - 대화 읽어오기 기능 개선
### Changed (변경사항)
- **불러오기 데이터 필터 분리**: `chat-input`에서 환경설정 텍스트 파트는 생략하고, 순수 대화 텍스트(`example1.txt`) 포맷만 로드하도록 규칙 간소화

---

## [v0.0.5] - 설정 파일 파서 구현
### Added (새 기능)
- **설정값 로드 및 바인딩**: `default-setting.md`에서 읽어온 초기 환경 설정값들(캡처 시간, 배터리, 방 이름, 배경색 등)을 대응되는 각 설정 박스 인풋에 매핑하는 파서 모듈 연동

---

## [v0.0.4] - UI 정렬 패치
### Changed (변경사항)
- **UI 일렬 정렬**: 중구난방이었던 환경설정 내 각 세부 조절 항목들의 세부 여백과 수평선을 깔끔하게 맞춘 CSS 디자인 개선

---

## [v0.0.3] - 설정 파일 분리
### Added (새 기능)
- **외부 설정 리소스 분리**: 기본 환경설정 프리셋 세팅값을 관리하기 위해 `default-setting.md` 파일을 리소스로 완전 분리

---

## [v0.0.2] - 코드 표준화 및 안정화
### Fixed (버그 수정)
- **HTML/CSS 문법 검증**: 어긋나 있던 마크업 태그들을 교정하고 웹 표준에 맞게 코드를 전체 보정
- **프로젝트 내부 명명 규칙 일치**: 파일 간 혼재되어 사용되던 변수 네이밍 컨벤션을 맞추고 가독성이 우수한 구조로 리팩토링 진행
- **성능 개선**: 불필요하게 잦은 DOM 탐색을 개선하고 선택자 캐싱을 일괄 도입하여 기동 효율 증대