# 📜 카카오톡 대화 생성기 (KakaoTalk Generator) 변경 이력 문서

이 문서는 카카오톡 대화 생성기 소프로젝트의 버전별 주요 변경 사항 및 업데이트 내역을 기록합니다.

## 📌 [미래 구현 예정 로드맵 (Roadmap)]
- **말풍선 꼬리 커스터마이징 (Speech Bubble Bezier Tail Refactoring)**:
  - 라이트모드/다크모드 실물 캡처본 포맷과 100% 동일한 곡률 및 베지어 꼬리 렌더링 보정 (현재 `git stash` 보관함 `wip: temp save tail code for theme refactoring`에 안전 보관 중).
- **컬러 피커 위치 및 팝업 최적화 (Custom Color Picker Position)**:
  - 입력 컴포넌트 하단 위치 지정 및 브라우저 기본 피커 미지원 시 커스텀 피커 포맷 대응.
- **스마트 헤더 고정 스크롤 정밀 조율 (Smart Scroll Guard)**:
  - 대화명을 포함한 고정 헤더 상단 영역(Y: 0~240px)의 레이아웃을 엄격 유지하며 마지막 말풍선 기준으로 가상 스크롤 이동.

## [v1.4.1] - 2026-08-09
- **대화방 테마 드롭다운 콤보박스(`select-theme`) 선택 시 메인 캔버스 100% 즉시 적용 완치**:
  - `gatherConfigFromUI()` 내 테마 선택(`light`, `dark`)별 8대 세부 색상 프리셋 연동 로직을 보정하여 테마 변경 시 `background-color`, `me-bubble-color`, `me-text-color`, `you-bubble-color`, `you-text-color`, `you-name-color`, `time-color`, `date-text-color`가 메인 캔버스에 100% 즉각 렌더링 반영되도록 완료.

## [v1.4.0] - 2026-08-09
- **테마 상세보기 모달 `✕` 닫기 버튼 동동 완치 & 실시간 예시 미니 미리보기 전용 렌더러 (`drawThemePreviewCanvas`) 탑재**:
  - `interface.js` 이벤트 위임에서 `#btn-close-theme-modal` 클릭 이벤트를 추가하여 모달 우측 상단 `✕` 닫기 버튼 클릭 시 모달이 100% 정상적으로 닫히도록 완치.
  - 모달 전용 360x460 미니 미리보기 렌더러(`drawThemePreviewCanvas`)를 신설하여 1080px 거대 헤더/아이콘 덮침 현상을 100% 제거하고 실제 핸드폰 카카오톡 화면처럼 수려하고 정교한 미니 렌더링 완성.

## [v1.3.9] - 2026-08-09
- **SPA 해시 라우팅 주소(`/#/small-project/KakaoTalk/index.html`) 진입 시 테마 상세보기 모달 유실 버그 완치**:
  - 싱글 페이지 라우터(`navigation.js`)가 HTML의 `<article>` 내부 요소만 추출 주입할 때 `<div id="theme-detail-modal">`이 누락되던 구조적 원인을 발견하여 모달 구조를 `<div id="kakaotalk-article">` 안쪽으로 완전 이동 배치.
  - `interface.js` 내 전역 이벤트 위임(Event Delegation) 및 body 텔레포트(`document.body.appendChild(modal)`) 로직을 장착하여 해시 라우팅 환경에서도 `z-index: 999999` 최상단 팝업이 100% 뜨도록 조치 완수.

## [v1.3.8] - 2026-08-09
- **셀 상태 및 와이파이 상태 0% 설정 시 캔버스 그림 100% 드로잉 렌더링 버그 완치**:
  - `interface.js` 내 OR Falsy 연산자(`parseInt(val) || 4`) 버그를 수정하여 `0` 지정 시 4(100%)로 튕기지 않고 `0`(0%)으로 정확히 수집되도록 조치.
  - `engine.js` 내 아이콘 렌더링 엔진의 0% 처리 조건부 칠하기(`wifiVal > 0`, `activeBars = cellVal`)를 교정하여 와이파이 및 셀 0% 설정 시 100% 아이콘 출력을 완전 소멸시키고 0% 꺼짐 상태로 정확히 드로잉 렌더링.

## [v1.3.7] - 2026-08-09
- **테마 상세보기 모달 100% 팝업 기동 완치 & 실물 캡처본 기준 프리셋 정밀 조정**:
  - `btn-open-theme-modal` 클릭 수신기(`setupThemeModalEvents()`)를 완벽 추가하여 **[상세보기 ⚙️]** 클릭 시 모달창이 100% 정상적으로 팝업 노출되고 실시간 미니 캔버스가 즉시 렌더링되도록 수정.
  - 라이트모드 실물 캡처본 기준으로 대화 시간 색상(`time-color: #64748b`), 상대 닉네임 색상(`you-name-color: #374151`) 정밀 보정 및 3인 이상 참여 시 대화방 이름 `"그룹채팅 5"` 자동 인원수 표시 동기화 연동.

## [v1.3.6] - 2026-08-09
- **`div#svg-box` 감싸는 불필요 wrapper 구조 제거 및 캔버스 자체 둥근 모서리(`border-radius: 20px`) 스타일링 반영**:
  - `index.html`에서 `<div id="kakaotalk-article">` 직계 자식으로 `<canvas id="chat-canvas">`가 배치되도록 DOM 구조 간소화.
  - `kakaotalk-style.css`에서 `#chat-canvas`에 `border-radius: 20px`, `box-shadow`, `border`, `overflow: hidden` 스타일을 직접 적용하여 수려한 모던 라운드 캔버스 표출.

## [v1.3.5] - 2026-08-09
- **대화 파일 로드 시 대화 범위(`1 ~ totalCount`) 및 진행 슬라이더 100% 자동 재조정 연동**:
  - 사용자 대화 내보내기 파일 선택 로딩 시, 이전 `1 ~ 2` 범위 고정 현상을 제거하고 불러온 대화의 전체 개수(`totalCount`, 예: 3,192개)에 맞춰 `startRangeIndex = 1`, `endRangeIndex = totalCount`로 범위 라벨 및 진행률 슬라이더가 100% 자동 동기화 조절되도록 완벽 구현.

## [v1.3.4] - 2026-08-09
- **`applySettingsToUI` DOM 안전 가드 적용 및 네트워크 404 콘솔 로그 완전 소멸**:
  - `applySettingsToUI` 내의 모든 DOM 프로퍼티 대입부(`input-capture-time`, `input-battery`, `input-bg-color` 등)에 `if (el)` 안전 검사 가드를 장착하여 `TypeError: Cannot set properties of null` 런타임 크래시 100% 완전 완치.
  - `loadDefaultData` 시 존재하지 않는 404 경로 fetch 시도를 소거하고 순수 인메모리 기본 데이터(세로 2340px)로 직통 렌더링 전환하여 브라우저 개발자 콘솔 404 빨간색 네트워크 로그 영구 소멸.

## [v1.3.3] - 2026-08-09
- **대화내용 불러오기 버튼 100% 동작 보장 및 OS 탐색기 기본 필터 모든 파일(*.*) 전환**:
  - HTML 파일 로더(`<input type="file" id="file-loader">`)의 `accept` 속성을 완전히 소거하여 윈도우 OS 파일 탐색기 창이 열릴 때 기본 필터가 **"모든 파일 (*.*)"**로 즉시 설정되도록 전환.
  - **[대화내용 불러오기]** 버튼 클릭 이벤트 수신기 강화 (`e.preventDefault()`, `fileLoader.value=''` 사전 초기화 후 `fileLoader.click()` 100% 호환 호출).

## [v1.3.2] - 2026-08-09
- **대화진행률 0/0개 미출력 및 캔버스 요소 렌더링 무효화 버그 수정**:
  - 파일 로드 시 대화진행률 슬라이더(`#input-progress`)의 `value` 기본값이 `-1`/`0`으로 구속되어 캔버스 렌더링 출력이 전면 스킵되던 현상 완전 해결.
  - 파일 로딩 즉시 `progressEl.value = totalCount` (3,192개)로 100% 자동 풀 로딩 대입되어 배터리, 와이파이, 채팅방 이름 및 전체 대화방 캔버스 드로잉 정상 가동.

## [v1.3.1] - 2026-08-09
- **대화 내보내기 파일 로딩 & 정규식 파싱 엔진 버그 수정**:
  - `index.html` 파일 로더의 `accept` 제한을 `*`로 변경하여 확장자가 없는 파일(예: `정재현... 대화`) 및 모든 텍스트 파일 100% 선택 지원.
  - 시간 표기가 결합된 날짜 헤더(`2022년 7월 8일 오후 1:24`) 파싱 정규식 교정 및 개행 멀티라인 메시지 연속 연결 처리 패치 (3,192개 대화 100% 완벽 로드).

## [v1.3.0] - 2026-08-09
- **대화방 테마 단일화 및 실시간 미니 프리뷰 색상 상세 설정 모달 도입**:
  - 메인 환경설정의 파편화된 색상 피커들을 **[대화방 테마] (기본 테마 / 다크 테마 / 사용자 지정)** 콤보박스로 100% 퉁쳐서 단일화.
  - 기본 테마 색상 설정: 배경색 `#acc0d1` (카카오 클래식 파스텔 블루), 내 말풍선 `#fee500`, 내 글씨 `#000000`, 상대 말풍선 `#ffffff`, 상대 글씨 `#000000`, 상대 닉네임 `#2c3e50`, 대화 시간 `#555555`, 날짜 글씨 `#444444`.
  - **[상세보기 ⚙️]** 버튼 클릭 시 팝업되는 **독립 세부 색상 설정 모달 창** 신설:
    - 모달 좌측에 실시간으로 변경 모습이 렌더링되는 **미니 예시 대화방 Live Preview 캔버스** 제공.
    - 모달 우측에서 7대 세부 색상(배경, 내말풍선, 내글씨, 상대말풍선, 상대글씨, 상대닉네임, 시간, 날짜)을 개별 조정 후 [적용] 시 '사용자 지정(Custom)'으로 자동 이관 및 메인 캔버스 100% 반영.

## [v1.2.2] - 2026-08-09
- **카카오톡 실물 캡처본 100% 동기화 말풍선 디자인 적용**:
  - 사용자 실물 캡처본(다크모드) 기준 디폴트 색상 100% 동일 적용: 배경색 `#000000`(딥 블랙), 내 말풍선 `#fee500`(카카오 시그니처 옐로우 / 텍스트 `#000000`), 상대 말풍선 `#2a2a2a`(카카오 다크 차콜 / 텍스트 `#ffffff`), 상대 닉네임 `#d1d1d6`, 대화 시간 `#8e8e93`.
  - 말풍선 둥근 모서리 곡률(Border Radius 36px) 및 우상단/좌상단 카카오 시그니처 라운드 꼬리 라인 100% 재현.

## [v1.2.0] - 2026-08-08
- 그림 다운로드 기능 확장 (PNG, MP4 비디오, 통 이미지 저장 지원)
- 모바일 브라우저 롱터치 이미지 저장 대응

## [v1.0.0] - 2026-08-01
- 최초 릴리즈
