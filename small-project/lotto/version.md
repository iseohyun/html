## v1.0.8
1. "🍀 누적 당첨 번호 (추첨기 히트맵 및 빈도 그래프)"의 빈도그래프에도
- "드라마틱하게", "드라이하게" 버튼을 구현하시오
- 최소/최대 선택을 구현하시오.
2. <!-- 시뮬레이션 정지 요약 모달 --> 이하의 코드가 DevTool에서 전혀 보이지 않을 가능성. 최소한 보이게 하려면 어떻게 해야하나? 조건과 상관없이 항상 보이도록 수정

## v1.0.7
메뉴얼로 바뀐 스타일을 수정하지 마시오.
1. "🎯 누적 선택 번호 (응모권 히트맵 및 빈도 그래프)"의 빈도그래프와 같은 방법으로
"🍀 누적 당첨 번호 (추첨기 히트맵 및 빈도 그래프)"의 빈도그래프를 개발
2. 여전히 시뮬레이션이 당첨되어 멈추었을 때 팝업이 등장하지 않는 문제. 스크롤이 어디있던간에 화면 정 중앙에 출력되어야 함.
3. 당첨 번호는 몇자리 숫자든 간에 동그란 원이어야 함(타원X)
4. "💬 이 문서에 대한 질문 및 건의"의 width가 온전히 한 단락으로 표시되도록 변경하시오


## v1.0.6
0. 메뉴얼 업데이트 반영
버그1. "드라마틱하게"의 기능구현이 잘 못 됨.
그래프의 최소값이 최소 선택된 번호의 선택횟수, 최대값이, 최대 선택된 번호의 선택횟수
버그2. 누적 당첨번호의 빈도그래프에도 누적 선택번호의 빈도그래프와 동일하게 그래프 교시 방법 및 최소/최대 선택 정보가 표시되어야 함.
버그3. 누적 수입 누락 (누작 수입 = 누적 당첨액 - 누적 지출액)
버그4. 1~5등의 당첨횟수 옆 괄호안에는 누적 당첨금액이 표시되어야 함.
버그5. 매주 응모 횟수가 늘어남에 따라 당첨번호의 실제 회차가 빠르게 소진되는 버그(응모 횟수가 늘어나도라도 시뮬레이션 속도에 맞춰서 당첨 번호가 변경되어야 함)
버그6. 당첨번호 표시 할 때, 당첨번호가 한자리숫자인 경우 당첨번호의 width가 작아지면서 전체적인 표기가 들쭉날쭉함.당첨번호가 1자리 여도 동일하게 2자리 넓이로 표시
버그7. 시뮬레이션 정지조건에도 팝업이 나오지 않는 문제

## v1.0.5
0. 메뉴얼 업데이트 반영
버그1. 시뮬레이션 정지조건일 때, 팝업이 나타나지 않는 문제
버그2. 해당 문서 마지막에 "이 문서에 대한 질문 및 건의"항목이 페이지 전체width가 아닌 일부만으로 표시되는 문제
- 기능추가: 누적 선택 번호의 빈도 그래프(세로막대)의 표시방법을 다음 2가지로 구분
a. "드라마틱하게": 최저값을 0, 최대값을 100으로 설정
b. "드라이하게" : 0을 0, 최대값을 100으로 설정
- 각 버튼은 그래프의 하단에 출력
- 버튼 하단에 최소/최대 선택 출력 (##번(##회) ~ ##번(##회))
- (빈도그래프+표시방법버튼+최대최소값)의 높이가 응모권 히트맴과 동일하도록 최대높이의 빈도그래프 작성
- 누적 담청 번호의 빈도 그래프에도 동일하게 적용
- "🍀 기댓값 대비 당첨 비율" 그래프의 height가 최대 높이로 설정

## v1.0.4
0. 메뉴얼로 변경된 UI 항목 반영
0-1. 별도의 지시 없이 git업데이트 금지
1. 히트맵에서 mouseover시 숫자만 출력할 때, 마우스 숨김
2. 시뮬레이션 속도 range의 좌우 반전(현재 긴 그래프가 더 느림)
-> 긴 값(큰값) = 빠름
3. 시뮬레이션 슬롯마다 초기화 버튼 삽입
4. 버그: 응모권에 아직 6개가 선택되지 않았음에도 메뉴얼로 추가 숫자가 선택되지 않는 버그

## v1.0.3
1. lotto.hmtl에서 반복적으로 등장하는 css에 대하여 lotto.css로 이관
2. 각 슬롯의 저장시 세팅방법 포함
3. 저장 이름은 "YYYYMMDD hh:mm:ss"로만 표기
4. 기본 1번 슬롯을 선택한것으로 간주
5. 빈 슬롯에서 "로드"가 클릭되면, 최초 설정으로 되돌리고, 편집슬롯이 해당슬롯으로 이해.
6. 만약 현재 슬롯에서 저장 없이 다른 슬롯으로 이동하려고 할 때, 현재 슬롯의 저장 여부를 물어봄
7. 누적 선택 번호의 빈도와 그래프에서 서로의 색이 일치하지 않는 문제(히트맵의 색으로 통일)
8. 그래프의 높이는 히트맵의 높이와 동일하게 설정
9. 히트맵에서 mouseover시 숫자만 출력

## v1.0.2
1. "시뮬레이션 제어"와 "시뮬레이션 분석결과"의 분리
2. 설정아이콘을 통한 설정 숨기기 기능 제거
3. 시뮬레이션 설정의 제목에 다음 항목이 들어감(아이콘으로 표현하는 것을 선호)
- (시뮬레이션) 시작/정지
- (시뮬레이션) 설정 초기화
4. 시뮬레이션 속도 UI변경: range로 입력. 기본값은 1주=1초(중앙값, default)
오른쪽 1step = 1주 고정, +1초
왼쪽 1step = 1초 고정, +1주
예) 오른쪽 3step = "1주 4초 표시"
    왼쪽 7step = "8주 1초 표시"
5. 시뮬레이션 결과 저장 기능 slot1~5
6.누적 선택 번호 세로막대그래프(누적 당첨 번호 세로막대그래프 동일)
- 각 그래프의 선택 횟수를 막대그래프로 표시
- 가장 많이 선택된 숫자의 막대그래프를 기준으로 최대값을 설정
- 그래프의 배치는 히트맵과 동일한 행에 배치
7. 누적 당청 횟수 세로 막대 그래프(누적 지출액이 표시되는 배너의 행에 배치)
- 가로축: 등수(1등, 2등, 3등, 4등, 5등)
- 세로축: 당첨 확률(당첨 횟수/ 당첨 기댓값)
- 세로축의 가장 큰 값을 최대값으로 삼음
- 100%에 보조선을 표시

## v1.0.1
1. 시뮬레이션 시작버튼의 width는 설정버튼이 열린 상태의 width와 항상 같다.
2. 시뮬레이션이 진행중인 경우 시뮬레이션 설정의 모든 항목은 disable된다.
3. 시뮬레이션 정지 옵션에서 체크된 항목에 의해 강제 정지 될 수 있다.
3-1. 시뮬레이션이 정지되었을 때, 다음 항목을 표시하는 팝업을 띄운다.
- 당첨 등수
- 당첨일
- 당첨액
- 누적 당첨액
- 누적 지출액
- 누적 손익
4. 시뮬레이션 시작일을 선택할 수 있다.
4-1. 시뮬레이션 시작일의 기본값은 오늘이다.
4-2. 시뮬레이션 시작일 ui를 클릭했을 때 day-picker를 출력하여 변경 할 수 있다.
5. 시뮬레이션 결과 항목을 신설한다.
시뮬레이션 결과 항목은 누적 지출액, 누적 당첨금액, 등수별 누적 당첨횟수를 보여준다.
5-1. 누적 선택
응모권과 동일한 배치의 UI에, 응모권에 많이 등장한 숫자는 붉은색, 적게 등장한 숫자는 회색으로 표시하여 그라데이션으로 표시하고, mouseover를 하였을 때, 총 등장횟수를 숫자로 보여준다.
5-2. 누적 당첨
응모권과 동일한 배치의 UI에, 당첨번호에 많이 등장한 숫자는 붉은색, 적게 등장한 숫자는 회색으로 표시하고, mouseover를 하였을 때, 총 등장횟수를 숫자로 보여준다.
6. 초기화
1. 기록초기화 버튼 생성
2. 세팅 초기화 버튼 생성