# 로또 시뮬레이터 버전 히스토리 (LLM 최적화 변경 이력)

> **적용 범위:** `/small-project/lotto/*`
> **지침 사항:** Git 커밋은 사용자의 명시적인 명령이 있을 때만 수행합니다. 모든 지침과 계획 사양은 통합되고 명확하며 구조화된 상태를 유지합니다.

---

## v1.0.20
- **[버그 수정]** 오프라인 환경이나 로컬 `file:///` 프로토콜 접속 시 브라우저 보안 규제(CORS 정책)로 인해 `fetch()`를 통한 파일 수급이 강제 차단되어 도움말 모달이 공백으로 나오던 심각한 구조적 한계를 극복하기 위해, 리뉴얼된 예쁜 도움말 카드 HTML 본문을 자바스크립트 내 정적 변수(`HELP_CONTENT_HTML`)로 내장하고, 클릭 시 즉각 문자열을 동적 주입(`innerHTML`)하도록 전환하여 네트워크 요청을 완전히 무력화하고 0ms 로딩 속도를 보장 완료.

## v1.0.19
- **[버그 수정]** 주소창 디렉토리 생략 접속 및 다차원 iframe 로드 환경에서도 로또 전용 도움말 문서가 안정적으로 탐색되도록 `/small-project/lotto` 감지 분기 강제 매핑 및 캐시 무력화 파라미터(`?t=timestamp`)를 연동하여 캐싱으로 인한 예전 도움말 파일 수급 문제를 완전 해결. 디버깅을 돕는 콘솔 경로 확인 로그 탑재.

## v1.0.18
- **[버그 수정]** 도움말 팝업 호출 시 브라우저 상대 경로 혼선으로 인해 루트의 공통 건의사항 양식(`c:\git\html\help.html`)이 로드되던 문제를, 실행 중인 페이지의 base URL 디렉토리를 동적으로 획득하여 로컬 폴더 전용 도움말 문서(`small-project/lotto/help.html`)를 확실하게 fetch하도록 경로 바인딩 보정 완료.

## v1.0.17
- **[버그 수정]** 모달 뒷면의 탭 메뉴 등 하단 엘리먼트들이 투명 레이어 간섭으로 인해 클릭되지 않던 버그를 `.lotto-modal`에 `pointer-events: none` 과 `.show` 활성 시 `pointer-events: auto` 규칙을 부여하여 완벽하게 해결.
- **[기능 개선]** '나만의 알고리즘' 고정 번호 추가 시, 로또 공의 색상을 해당 번호 범위별 당첨 공 색상 규칙(1~9:노랑, 10~19:파랑, 20~29:빨강, 30~39:회색, 40~45:초록)과 완벽하게 일치되도록 렌더링 개선.
- **[기능 개선]** '나만의 알고리즘' 응모 개수(P) 입력 도중 매주 응모 횟수 한도를 넘지 않도록 실시간 한계(Max) 값을 제한하고 초과분 자동 깎임 로직 보강.
- **[기능 추가]** '나만의 알고리즘' 행 추가(+) 버튼 클릭 시, 매주 응모 횟수에서 기존 행들의 응모 개수 총합을 제외한 잔여 최대 가능 개수를 자동으로 계산하여 기본값으로 입력해주는 기능 탑재.
- **[버그 수정]** 히트맵 셀 위에 마우스를 올릴 때 마우스 커서가 숨겨지던 CSS 규칙(`cursor: none;`)을 영구 소거하여 커서 가시성 확보.
- **[기능 추가]** 설정 초기화 버튼 우측에 도움말 버튼(`?`)을 추가하고, `help.html`의 리소스 가이드를 비동기로 읽어와 모달 창에 이쁘게 출력해주는 도움말 팝업 연동 완수.
- **[기능 개선]** `help.html` 문서의 질문-답변 디자인 레이아웃을 카드 뷰 및 웹폰트/배지 등이 가미된 미려하고 가독성 높은 현대적 디자인으로 대대적 리브랜딩 개정.

## v1.0.16

- **[기능 추가]** '나만의 알고리즘' 옵션 선택 시 맞춤 조합 규칙을 수립할 수 있는 `#algorithm-setting-modal` UI 모달 구현.
- **[기능 추가]** 모달에서 설정 행 추가(+) 및 삭제, 최근 N회차 당첨 빈도 TOP M개, 고정 숫자, 랜덤 L개 조합 빌딩 제어 구현.
- **[기능 추가]** M과 고정 번호의 합이 6에 도달할 때 인터페이스를 비활성화(disabled)하고, 수치 임의 조절로 6개 초과 발생 시 고정 번호 초과분을 우측 끝부터 자동으로 소거 및 정합성 강제 유지 처리.
- **[기능 추가]** 각 행의 응모 개수 P의 합산이 매주 응모 횟수(`times_a_week`)와 불일치 시 적용 경고 안내 가드 마련.
- **[기능 추가]** 시뮬레이션 매 주차 응모 시 설정된 행별 규칙을 순차 적용하여 가상 추첨 이력(최근 N회 빈출) 및 랜덤 풀로 6개 조합 발급 후 시뮬레이션에 주입하는 조합 연산 엔진 완성.
- **[기능 추가]** 슬롯 저장(`saveSlot`) 및 로드(`loadSlot`) 시 알고리즘 공식 세팅 데이터(`myAlgorithmSettings`)도 함께 백업/복원 연동 완료.

## v1.0.15

- **[기능 추가]** '시뮬레이션 시작' 및 '추첨일' 인풋 우측에 각각 요일(예: `(화)`, `(토)`)을 출력해 주는 독립형 텍스트 인디케이터 신설 및 동기화 구현.
- **[기능 추가]** 추첨일 피커 우측에 `(+yy/mm/dd)` 포맷으로 가상으로 경과된 년/월/일 세월을 연산하여 실시간 출력해 주는 경과 인디케이터 추가.

## v1.0.14

- **[기능 추가]** 추첨일 ID 속성 중복 오류 해소 및 전용 `simulation-draw-date` 요소를 활용한 날짜 렌더링 체계 구성.
- **[기능 추가]** '시뮬레이션 시작' 날짜에 연동하여 이를 포함하는 가장 가까운 다음 토요일을 연산하는 기본 추첨일 알고리즘 보완 및 1회 추첨당 +7일씩 누적 갱신 적용.
- **[기능 추가]** '시뮬레이션 시작' 날짜 조절(onchange) 시 실시간으로 추첨일 정보가 재연산 갱신되도록 양방향 동기화 연동.
- **[기능 추가]** 슬롯 저장(`saveSlot`) 및 로드(`loadSlot`) 시 시뮬레이션 시작일 정보 외에 개별 추첨일 날짜 정보(`drawDate`)도 함께 저장 및 로드할 수 있도록 직렬화 세션 확장.

## v1.0.13

- **[기능 추가]** 시뮬레이션 시작일 노출 방식을 '오늘' 텍스트 레이블 및 비활성화(disabled)된 날짜 피커 노출 구조로 리펙토링.
- **[기능 추가]** 매 시뮬레이션 회차(`count`) 추첨 완료 시마다 '오늘' 기준 다음주 토요일 날짜부터 매주 7일씩 가산되는 가상 추첨일 연산 로직(`getCalculatedDrawDate`)을 이식하고 시작일 인풋 value에 동적 갱신 노출.
- **[기능 추가]** 시뮬레이션 일시정지 모달에 계산된 가상 추첨일(토요일) 날짜가 당첨일로 연동되도록 매핑.
- **[기능 추가]** 슬롯 저장(`saveSlot`) 기능 실행 시, 대상 슬롯에 사용자 커스텀 편집 이름(`customName`)이 이미 존재한다면 스탬프 덮어쓰기 없이 해당 명칭을 그대로 계승 유지하도록 보존 처리.

## v1.0.12

- **[기능 추가]** 각 슬롯의 저장 명칭을 커스텀하게 편집/수정할 수 있도록 ✏️ (이름 수정) 액션 버튼 및 `editSlotName()` 기획 이식.
- **[기능 추가]** 로컬스토리지 내 직렬화 객체에 `customName` 속성을 연계하고 `updateSlotsUI()` 를 통한 실시간 UI 명칭 변경 동기화 구현.

## v1.0.11

- **[기능 추가]** 요약 통계 행(stats-row) 배경에 '누적 지출액', '누적 당첨금액', '누적 수입(절대값)', '누적 기댓값'의 상대적 크기를 오른쪽에서 왼쪽으로 채워지는 가로 막대 그래프 형태로 오버랩 렌더링 구현.
- **[기능 추가]** 4개 통계 지표의 막대 색상을 각 요약 텍스트 컬러와 매칭하여 가독성 강화.
- **[기능 추가]** 금액 포맷터(`numberToText`) 개선을 통해 억 단위 이상인 경우 소수점 2자리(예: `X.XX억`)까지 출력되도록 조정.

## v1.0.10

* **[리팩토링]** HTML 인라인 스타일 태그들을 중앙 집중식 `lotto.css` 유틸리티 클래스로 이관 완료 (HTML 용량 730 바이트 다이어트 달성).
* **[리팩토링]** 변수 및 함수명을 엄격한 클린 코드 원칙에 맞게 개선 (`auto` ➔ `isSimulationActive`, `lock` ➔ `isSettingsLocked` 등).
* **[리팩토링]** 미사용 CSS 클래스(`.tooltip`) 제거 및 레이아웃 오버헤드 제로화를 위한 규칙 그룹 재조직.
* **[메트릭]** 코드 용량 분석:
* `lotto.html`: 535라인 / 29,559 바이트
* `lotto.css`: 1,024라인 / 19,427 바이트
* `lotto.js`: 1,798라인 / 56,552 바이트

## v1.0.9

* **[기능 추가]** 정지 모달의 확인 버튼 클릭 시, 시뮬레이터 루프가 자동으로 재개되도록 기능 구현.
* **[기능 추가]** 통계 행(stats-row) 내에 동적 기댓값 ($E_{\text{week}} = \sum (\text{probs} \times \text{price})$) 표시 기능 구현.
* **[기능 추가]** 직전 토요일의 실제 추첨일을 기준으로 최근 추첨일 및 당첨 번호 표시 기능 추가.
* **[기능 추가]** 전용 행 내에 실제 DB 번호 다운로드 옵션 추가 (.csv 내보내기 기능).
* **[성능 개선]** 정적 툴팁 바에 즉각적인 `mouseenter`/`mouseleave` 이벤트를 매핑하여 툴팁 지연(lag) 현상 해결.

## v1.0.8

* **[기능 추가]** 누적 당첨 번호 막대 차트에 "드라마틱하게(dramatic)" 및 "드라이하게(dry)" 모드 토글 제어 기능 구현.
* **[기능 추가]** 누적 당첨 빈도 차트에 최소/최대 선택 범위 제한 기능 구현.
* **[버그 수정]** 개발자 도구(DevTools)에서 상시 접근이 가능하도록 시뮬레이션 정지 모달의 마크업 위치를 article 노드 내부로 재배치.

## v1.0.7

* **[기능 추가]** '나의 번호 빈도 차트'와 대응되는 '누적 당첨 빈도 막대 차트' 추가 구현.
* **[버그 수정]** 정지 모달이 뷰포트 정중앙에 엄격하게 정렬되도록 스크롤 오프셋 오작동 수정.
* **[스타일]** 당첨 번호 볼(sphere)이 자릿수와 관계없이 완벽한 원형을 유지하도록 정렬 스타일 보정.

## v1.0.6

* **[버그 수정]** 드라마틱 모드의 범위 파라미터가 정확한 로컬 최소값/최대값을 사용하여 정규화되도록 로직 교정.
* **[버그 수정]** 각 당첨 등수별 집계 칸 바로 옆에 순수익 계산 결과 및 지급액 메트릭이 함께 표시되도록 수정.
* **[버그 수정]** 시뮬레이션 틱(tick)에 맞춰 추첨 간격이 동기화되도록 회차 소모율 수정.

## v1.0.5

* **[기능 추가]** '나의 번호' 통계 데이터를 위한 "드라마틱하게" 및 "드라이하게" 빈도 차트 제어 옵션 추가.
* **[기능 추가]** '나의 번호' 히트맵 그리드와 일치하도록 빈도 차트 패널의 높이 동기화 설정.
* **[버그 수정]** 정지 모달의 디스플레이 트리거 누락 현상 해결.

## v1.0.4

* **[기능 추가]** 속도 조절 슬라이더의 UI 수치 표기 추가.
* **[기능 추가]** 슬롯 1~5의 개별 초기화 제어 버튼 추가.
* **[버그 수정]** 선택한 '나의 번호' 개수가 6개 미만일 때 번호 선택 덮어쓰기가 오작동하던 문제 해결.

## v1.0.3

* **[리팩토링]** `lotto.html`에 반복적으로 사용되던 인라인 스타일을 `lotto.css`로 대거 이관.
* **[기능 추가]** 슬롯 1~5의 로드/저장 동작 시 상태 변수들이 유기적으로 처리되도록 구성.

## v1.0.2

* **[기능 추가]** 컨트롤 패널 컴포넌트와 분석 패널 컴포넌트의 영역 분리.
* **[기능 추가]** 슬롯 1~5 설정 정보를 보존하기 위한 스토리지 메커니즘 구현.
* **[기능 추가]** 초기 확률 기댓값 그래프 레이아웃 도입.

## v1.0.1

* **[기능 추가]** 초기 시뮬레이션 루프, 시작/정지 옵션 파라미터, 시작일 설정 및 결과 표시기 구현.